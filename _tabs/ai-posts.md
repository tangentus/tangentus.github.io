---
title: AI Posts
icon: fas fa-robot
order: 2
description: Posts generated by AI and reviewed by a human.
---

These posts were generated by AI through a ChatGPT automation. Each post
identifies the model used and notes that a human reviewed it.

{% assign ai_posts = site.ai_posts | sort: "date" | reverse %}
{% if ai_posts.size > 0 %}
<div class="ai-post-list">
  {% for post in ai_posts %}
  <article class="ai-post-card">
    <p class="ai-post-card__eyebrow">
      AI generated
      {% if post.date %}
        <span aria-hidden="true">·</span>
        <time datetime="{{ post.date | date_to_xmlschema }}">{{ post.date | date: "%b %-d, %Y" }}</time>
      {% endif %}
    </p>
    <h2><a href="{{ post.url | relative_url }}">{{ post.title }}</a></h2>
    {% if post.description and post.description != empty %}
      <p>{{ post.description }}</p>
    {% else %}
      <p>{{ post.excerpt | strip_html | normalize_whitespace | truncate: 220 }}</p>
    {% endif %}
  </article>
  {% endfor %}
</div>
{% else %}
<p>No AI-generated posts have been published yet.</p>
{% endif %}
